---
title: "Homework Template"
author: "First Last"
date:   "`r format(Sys.Date(), '%B %d, %Y')`"
output:
  html_document:
    theme: united
    code_folding: show
    toc: true
    style: BS831.css
    toc_float: true
---

## Settings

```{r settings, warning=FALSE, message=FALSE}
knitr::opts_chunk$set(echo = TRUE, message=FALSE, warning=FALSE)

library(Biobase)
library(BS831)

OMPATH <- Sys.getenv("OMPATH")
if ( OMPATH=="" ) stop( "OMPATH must be set" )
print(OMPATH) 
DATA <- Sys.getenv("DATAPATH")
```

# Exercise 1: Creating an ExpressionSet from multiple parts

```{r}
EXP <- as.matrix(readxl::read_xls(file.path(DATA, "hw1/ESet_exprs.xls")))[,2:101]
PDAT <- readxl::read_xls(file.path(DATA, "hw1/ESet_pData.xls")) # data.frame(t(readxl::read_xls(file.path(DATA, "hw1/ESet_pData.xls"))))
FDAT <- readxl::read_xls(file.path(DATA, "hw1/ESet_fData.xls"))

ESET <- Biobase::ExpressionSet(assayData = EXP, phenoData = AnnotatedDataFrame(PDAT),
                               featureData = AnnotatedDataFrame(FDAT))

gMedian <- compute gene-specific (i.e., row) median
sMedian <- compute sample-specific (i.e., column) median
```

# Exercise 2: Median vs. MAD plot

```{r}

```

Exercise 3: "Genotype" - Phenotype Association

```{r}

```

# Exercise 4: Bayes Thereom

```{r}

```

# Exercise 5: Matrix Operations

```{r}

```
